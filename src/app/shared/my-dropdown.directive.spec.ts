import { MyDropdownDirective } from './my-dropdown.directive';

describe('MyDropdownDirective', () => {
  it('should create an instance', () => {
    const directive = new MyDropdownDirective();
    expect(directive).toBeTruthy();
  });
});
